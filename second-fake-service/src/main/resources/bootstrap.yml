spring:
  application:
    name: FIRST-FAKE-SERVICE
  boot:
    admin:
      client:
        url: http://localhost:8585
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/

server:
  port: 8687